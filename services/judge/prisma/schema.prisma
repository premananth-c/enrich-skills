generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Reuses same schema as API - run from api or use shared schema
// For now judge connects to same DB
